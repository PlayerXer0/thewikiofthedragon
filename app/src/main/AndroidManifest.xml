<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Thewikiofthedragon">

        <!-- Actividad del Bando Negro -->
        <activity
            android:name=".BlackFactionActivity"
            android:exported="false" />

        <!-- Actividad de los Personajes del Bando Negro -->
        <activity
            android:name=".BlackFactionCharactersActivity"
            android:exported="false" />

        <!-- Actividad del Bando Verde -->
        <activity
            android:name=".GreenFactionActivity"
            android:exported="false" />

        <!-- Actividad principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>

        <!-- LoginActivity como actividad inicial -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad de creación de perfil -->
        <activity
            android:name=".PerfilUsuarioActivity"
            android:exported="false" />

    </application>

</manifest>
